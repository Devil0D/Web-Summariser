# ── server ────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9     # required for Form() and File() in FastAPI

# ── your existing ML deps (keep whatever versions you already have) ───────────
transformers>=4.40.0
torch>=2.0.0
sentencepiece               # T5 tokenizer
accelerate                  # optional but speeds up CPU inference

# ── LexRank ───────────────────────────────────────────────────────────────────
sumy==0.11.0
nltk                        # run once: python -c "import nltk; nltk.download('punkt')"

# ── file handling ─────────────────────────────────────────────────────────────
pypdf>=4.0.0                # PDF extraction (same lib you already use)

# ── optional: URL fetching (only needed if you want server-side fetch) ────────
# httpx==0.27.0
